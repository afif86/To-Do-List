import { useParams } from "react-router-dom";

const TodoDetails = () => {
    const TodoDetails = () => {
        const{} = useParams();
    }
    return (
        <div className="todo-details">
            <h2>Todo details</h2>
        </div>
    );
}
 
export default TodoDetails;  